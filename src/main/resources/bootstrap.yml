service:
  open-ia:
    key: <YOUR-API-KEY>

server:
  port: 8080

logging:
  level:
    root: error

spring:
  main:
    banner-mode: "off"
    application:
      group: open-ia
      name: open-ia-poem-generator
